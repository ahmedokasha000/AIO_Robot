global_costmap:
  global_frame: map
  robot_base_frame: base_footprint
  update_frequency: 2.0  #if 0.0 this mean to work when new goal is received or the local planner reports that its path is blocked only
  publish_frequency: 2.0
  static_map: true
  rolling_window: true
  width: 22.0
  height: 22.0
  resolution: 0.05
